#!/usr/bin/env node
import fs from 'node:fs';
import vm from 'node:vm';
import assert from 'node:assert/strict';
const read=p=>fs.readFileSync(p,'utf8');
const bug=read('app-menu-bug-report.js');
const beg=bug.indexOf('function getBugReportAccount() {'),end=bug.indexOf('function getBugReportBuildVersion() {',beg);
assert(beg>=0&&end>beg,'bug report account function');
const ctx={window:{rakUserProfileGet:()=>({accountNumber:'1234',fullName:'Novák Jan'})},app:{activeAccountId:'5678',activeAccountName:'Fallback Test'}};
vm.runInNewContext(bug.slice(beg,end)+'\nthis.getAccount=getBugReportAccount;',ctx);
let person=ctx.getAccount();
assert.equal(person.id,'1234');assert.equal(person.name,'Novák Jan');
ctx.window.rakUserProfileGet=()=>null;
person=ctx.getAccount();assert.equal(person.id,'5678');assert.equal(person.name,'Fallback Test');
ctx.app.activeAccountId='';assert.equal(ctx.getAccount(),null,'do not substitute discontinued game profile');
assert(!/herním profilu|<option>Hra<\/option>|report\.game|activeGameShell|game: String/.test(bug),'bug report has no game references');
assert(bug.includes('Nejdřív se přihlas do RaK.')&&bug.includes('accountId: account ? String(account.id'),'report still includes employee identity');
const dash=read('dashboard.js');
assert(dash.includes('vzhledu profilu a kontrolu aktualizace')&&!dash.includes('herních statistik a kontrolu aktualizace'),'sync tooltip');
assert(!dash.includes("step('herni-profily'")&&!dash.includes("step('herni-statistiky'")&&!dash.includes('gamesRefreshRemoteLeaderboards'),'retired sync steps removed');
assert(dash.includes('window.rakUserProfileGet')&&dash.includes('loadActiveAccountUiRemoteSettings(id)'),'appearance remote sync retained');
const pages=read('app-menu-pages.js');
assert(pages.includes('buildRakProfileSettingsHtml()')&&!pages.includes('const profileCard = buildGamesProfileSettingsHtml()'),'native profile');
assert(!pages.includes('gamesRenderAccountChips()')&&!pages.includes('renderGamesProfileStatus()'),'retired chips');
assert(!pages.includes('odstranily se Hry')&&!pages.includes('Hry a online profily'),'current About has no retired features');
const ui=read('ui.js');
assert(!ui.includes('function openGamesRankModal(')&&!ui.includes('function renderGamesProfileStatus(')&&!ui.includes('function buildGamesProfileSettingsHtml('),'dead game screen removed');
assert(ui.includes('function buildAppHistoryHtml(versionText)')&&ui.includes('workSections.map(section'),'fallback history filtered');
assert(!read('app-bottom-nav.js').includes('games: () => { openGamesPage(); }'),'nav action removed');
assert(!read('app-actions.js').includes("'open-game':"),'game click dispatch removed');
const health=read('app-health-audits.js');
assert(!health.includes("    '#games'")&&!health.includes("    'styles-games.css'")&&!health.includes("'kalkulacky', 'games', 'menu'"),'no false games health requirements');
const post=read('app-postload-audits.js');
assert(!post.includes('runPhaseFiveGamePerformanceAudit()')&&!post.includes('runGameEngineBaselineAudit()'),'no obsolete background game audits');
const menu=read('app-menu.js');
assert(menu.includes('nových účtů pracovníků:')&&!menu.includes('nových herních profilů:'),'new employee account label');
assert(menu.includes('.filter(line => !/(?:herní|herni|piškvorky|lodě|online hry|top.?score|leaderboard|game[_ -]'),'diagnostics hides retired-feature lines');
assert(menu.includes('ensureGameAccountsExistForWorkers(workerSettings.workers)'),'creating real employee accounts must survive');
const user=read('rak-user-profile.js');
assert(user.includes("const ACCOUNT_UI_PROFILE_KEY = 'rotace_kalkulacky:games_profile_v1';"),'existing appearance storage key preserved');
assert(user.includes('window.gamesGetProfile = getAccountUiProfile')&&user.includes('window.gamesSaveProfile = saveAccountUiProfile'),'appearance compatibility preserved');
assert(read('rak-account-access.js').includes("from('game_accounts')"),'real employee login table retained');
const driver=read('tools/shift-report-mo-hotfix-170-smoke.mjs');
assert(driver.includes('// RAK_17021_TWO_PASS_GUARD')&&driver.includes('const already17021=')&&driver.includes('const old=already17021?'),'second-pass build handles release');
const config=read('supabase-config.js'),sw=read('sw.js'),index=read('index.html');
assert(config.includes('https://cgshssdjgzzuprlwnabl.supabase.co')&&!config.includes('bkqamcbkiwumsvelahxr'),'test DB only');
assert(config.includes('window.RAK_RELEASE_VERSION = "1.7.21";')&&config.includes('window.RAK_PWA_BUILD = "v1.7.21-gamescleanup1";'),'release version');
assert(index.includes("var build='v1.7.21-gamescleanup1';")&&sw.includes("const CACHE_VERSION = 'v1.7.21';"),'cache and index version');
assert.equal(JSON.parse(read('package.json')).version,'1.7.0','technical version unchanged');
console.log('[games-cleanup-17021-smoke] OK current identity and bug report, no game UI or phantom audits, dashboard/profile/appearance intact, two-pass build, test DB, version 1.7.21');
