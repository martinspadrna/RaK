# Non-production migration history

These SQL files are retained for auditability and historical regression tests only.
Supabase does not execute files outside `supabase/migrations`.

The active directory is locked to the exact production migration history recorded by
project `bkqamcbkiwumsvelahxr`. New production-bound migrations must be created as new
files after the locked baseline; historical files must never be renamed or edited.
